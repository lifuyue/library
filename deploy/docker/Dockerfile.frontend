FROM nginx:stable-alpine
# 工具：为健康检查提供 curl
RUN apk add --no-cache curl

# 复制本机已构建的前端产物（frontend/dist）
COPY frontend/dist /usr/share/nginx/html

# 暴露端口
EXPOSE 80

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]
